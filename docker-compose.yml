services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: dativa
      POSTGRES_USER: dativa
      POSTGRES_PASSWORD: dativa
    ports:
      - "5432:5432"
    volumes:
      - dativa_pg:/var/lib/postgresql/data

volumes:
  dativa_pg:
